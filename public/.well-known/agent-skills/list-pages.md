# list_pages

Returns all available DietMate documentation pages with their titles, URLs, and sections.

## Input

No input required.

## Output

Array of all documentation pages, each with `title`, `url`, and `section`.
